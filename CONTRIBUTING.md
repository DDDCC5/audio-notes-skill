# 贡献指南

欢迎扩展转写适配器、场景模板和测试。先建立虚构样例，不提交任何真实录音、密钥或组织资料。

提交前执行：`python -m unittest discover -s tests -v`。若更改 Schema，同时更新脚本、示例、契约和测试。若更改服务商接入，需要上传同意门槛、endpoint 显示、错误脱敏和真实测试状态说明。

不要将模拟测试通过写成真实转写质量通过。修改依赖后在支持系统中验证。发布前维护者应核对 MIT 版权主体并审查 ZIP/提交文件清单。
