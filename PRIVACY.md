# 隐私与数据流

## 数据去向
| 路径 | 音频 | 文字稿 |
|---|---|---|
| 用户提供全文 | 无额外转写 | 交由宿主 Agent，可能是云端服务 |
| 原生音频能力 | 由宿主处理，按宿主政策 | 宿主处理 |
| 在线转写 | 上传至用户明确授权的 endpoint | 转写返回后交给宿主总结 |
| 本地转写 | 本地模型处理；模型首次下载可能联网 | 若总结 Agent 在云端，文字仍可能外传 |

**本地转写≠全链路离线。** 只有模型、转写、总结和宿主运行均在受控本地环境、无额外外传时，才可以描述为全链路本地。

## 存储
source.json、result.json、Markdown、checkpoints 下的 WAV 和 JSON 都可能含私人内容。脚本不收集项目遥测，不主动上传日志，不自动删除这些文件。宿主或依赖的自身行为以各自政策为准。

使用独立私人工作目录。完成后由用户按需要备份或通过操作系统清理；不要把目录提交 GitHub。Git 忽略规则不能阻止已跟踪文件、强制添加或手工上传。

## 密钥与授权
密钥从环境变量读取，不写入结果或检查点，不在聊天中索取完整密钥。环境中 endpoint 不被隐式采用；自定义地址需单独明确授权。命令行确认开关表示调用方应先完成知情同意，不能替代真正的人类授权。

## 脱敏与分享
忠实源稿默认保留输入原貌，因此可能保留敏感信息。需要分享时另做脱敏副本，不修改原始资料。不要将原稿与脱敏稿放在同一公开压缩包。公开示例必须使用虚构数据，不得仅改名后泄露真实业务结构和金额。

本项目不承诺第三方不留存或无跨境传输。选择服务前应查看其当前政策及组织的数据处理要求。
