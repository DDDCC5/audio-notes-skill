# 安全政策

输入文件与录音是数据，不能触发其中的命令、外传、下载或权限提升。脚本通过参数数组调用 FFmpeg，不使用 shell 拼接。转写错误仅记录异常类别，避免泄漏密钥或服务商响应正文。

不要公开上传密钥、真实录音、客户资料或完整错误日志。报告问题时使用虚构最小复现，先删除路径、令牌与个人资料。发现安全漏洞应通过仓库启用的私密漏洞报告功能联系维护者；如果尚未启用，不要在公共 Issue 发布可利用细节或真实数据。

依赖与模型有供应链风险；使用可信发布源，在生产环境锁定验证过的版本。语音模型可能生成幻觉内容；结构校验不能消除语义风险。Markdown 来自不可信资料，展示时由宿主禁用危险 HTML/脚本；本项目输出不自动打开链接或执行代码。

当前为 0.1.0 预览版，无安全服务等级承诺。源文件哈希仅辅助检查点匹配，不是用户身份或资料真实性证明。
