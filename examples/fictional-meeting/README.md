# 虚构示例

此目录的角色、组织、事件、日期表达、数字和文本均为演示而虚构，与用户提供的真实材料无关。

输入 input.txt，经标准库导入生成 source.json。result.json 是人工构建的预期示例，用于演示 Agent 应如何填写契约，**不是自动总结模型的实测输出**。三个 Markdown 由脚本从该结果渲染。

重点演示：
- “考虑下周二上线”是提议，不是已决定发布日期。
- 周五修复与明天发文案是明确承诺；没有会议日期，不能转成具体年月日。
- 三百元是初步估计，不能写成已批准预算。
- AI 风险分析单独呈现，附来源和局限。
